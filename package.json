{
  "name": "play",
  "version": "1.0.0",
  "description": "",
  "main": "lib/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf lib/ && npm run build && node lib/institutionTagsSolution.js",
    "build": "tsc",
    "cont:build": "docker build -t cloud_run .",
    "cont:start": "docker run -p 3001:3000 -d --name cloud_run -e \"PG_HOST=host.docker.internal\" cloud_run",
    "cont:stop": "docker stop cloud_run && docker rm cloud_run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
