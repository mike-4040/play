{
  "name": "play",
  "version": "1.0.0",
  "description": "",
  "main": "lib/app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --watch & nodemon --watch lib",
    "build": "tsc",
    "cont": "docker build -t play . && docker run -p 3000:3000 --name play -e \"PG_HOST=host.docker.internal\" --rm play"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.2.5",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "fetch-mock": "^9.11.0",
    "nodemon": "^2.0.20"
  }
}
